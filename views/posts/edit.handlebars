 <!-- START ARTICLE FEED -->
        <section class="articles">
            <div class="column is-8 is-offset-2">
                <!-- START ARTICLE -->
                <div class="card article">
                    <div class="card-content">
                        <form action="/posts/edit/{{blogpost._id}}?_method=PUT" method="POST" enctype="multipart/form-data">
                            <div class="field">
                                <label class="label">Title</label>
                                <div class="control">
                                    <input class="input" type="text" id="title" name="title" placeholder="Title" value="{{blogpost.title}}">
                                </div>
                            </div>

                            <div class="field">
                                <div class="file">
                                    <label class="file-label">
                                        <input class="file-input" type="file" name="image" id="image" value="{{blogpost.image}}">
                                        <span class="file-cta">
                                        <span class="file-icon">
                                            <i class="fas fa-upload"></i>
                                        </span>
                                        <span class="file-label">
                                            Choose a file…
                                        </span>
                                        </span>
                                    </label>
                                </div>
                            </div>
                            <div class="field">
                                    <div class="field">
                                        <label class="label">Body</label>
                                        <div class="control">
                                            <textarea class="textarea" id="body" name="body" placeholder="Textarea">{{blogpost.body}}</textarea>
                                        </div>
                                    </div>
                            </div>
                            <div class="field">
                                  <button type="submit" class="button is-info">Submit</button>
                            </div>
                         </form>
                     </div>
                </div>
            </div>
        </section>